<UserControl x:Class="FFXIV_TexTools.Views.ExportModelOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FFXIV_TexTools.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <UniformGrid Rows="1">
        <Button Content="Export DAE + Materials" VerticalAlignment="Center" Margin="10" Command="{Binding ExportDaePlusMaterialsCommand}"/>
        <Button Content="Export DAE file" VerticalAlignment="Center" Margin="10" Command="{Binding ExportDaeCommand}"/>
        <Button Content="Export OBJ files" VerticalAlignment="Center" Margin="10" Command="{Binding ExportObjCommand}"/>
    </UniformGrid>
</UserControl>
