<UserControl x:Class="FFXIV_TexTools.Views.ImportTextureOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FFXIV_TexTools.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UniformGrid Rows="1">
        <Button Content="Import DDS" VerticalAlignment="Center" Margin="10" Command="{Binding ImportDDSButton}" IsEnabled="{Binding DDSImportEnabled}"/>
        <Button Content="Import From..." VerticalAlignment="Center" Margin="10" Command="{Binding ImportFromButton}"/>
        <Button Content="Import BMP" VerticalAlignment="Center" Margin="10" Command="{Binding ImportBMPButton}" IsEnabled="{Binding BMPImportEnabled}"/>
    </UniformGrid>
</UserControl>
